{
  "name": "retake-project",
  "version": "1.0.0",
  "description": "Retake Project",
  "main": "index.js",
  "author": "Sergio Boffi",
  "license": "MIT",
  "scripts": {
    "start": "node app.js",
    "dev": "DEBUG=router:* nodemon --delay 1 --ignore public/ --ignore client/ --ignore app.js",
    "build": "npm --prefix client install && npm --prefix client run build",
    "test": "mocha --exit",
    "watch:test": "mocha -w -min --delay=2000"
  },
  "dependencies": {
    "chess.js": "^1.0.0-beta.8",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "method-override": "^3.0.0",
    "mongodb": "^6.2.0",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
